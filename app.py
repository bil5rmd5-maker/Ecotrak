# --- 8. قسم التحليل الاستراتيجي وصناعة القرار (Decision Support System) ---
st.markdown("---")
st.subheader("🧠 المستشار الذكي: تحليل الأسباب والحلول")

col_analysis, col_actions = st.columns(2)

with col_analysis:
    st.info("🔍 **تحليل أسباب الحالة الحالية:**")
    
    # سيناريو 1: خطر بسبب تكلفة الطلب العالية
    if order_cost > 500:
        st.write("- **سبب الخسارة المحتمل:** تكاليف الشحن والطلب (S) مرتفعة جداً، مما يلتهم هوامش الربح عند تكرار الطلب.")
    
    # سيناريو 2: خطر بسبب تكلفة التخزين العالية
    if holding_cost > (price * 0.1):
        st.write("- **سبب الخسارة المحتمل:** تكلفة تخزين القطعة (H) تتجاوز 10% من سعرها، مما يعني أن بقاء البضاعة لفترة طويلة في المستودع يسبب خسارة مادية مباشرة.")
        
    # سيناريو 3: خطر بسبب نقص المخزون
    if current_stock < reorder_point:
        st.write(f"- **سبب القرار:** المخزون الحالي ({current_stock}) أقل من نقطة الأمان ({int(reorder_point)})، مما يهدد بتوقف المبيعات وضياع فرص ربح تقدر بـ {potential_loss:,.0f} ريال.")

    if current_stock > (eoq * 2):
        st.write("- **ملاحظة:** لديك فائض كبير في المخزون (Overstocking)، مما يعطل رأس مال الشركة في بضاعة مركونة.")

with col_actions:
    st.success("🛠️ **ماذا يجب عليك أن تفعل الآن؟**")
    
    if current_stock <= reorder_point:
        st.write(f"1. **إجراء فوري:** قم بإصدار أمر شراء بـ {int(eoq)} وحدة فوراً.")
        st.write(f"2. **التفاوض:** حاول التفاوض مع المورد لتقليل وقت التوريد (Lead Time) لتقليل مستوى الأمان المطلوب.")
    else:
        st.write("1. **المراقبة:** الوضع مستقر، استمر في مراقبة حركة السحب اليومية.")
        st.write("2. **التحسين:** استغل استقرار المخزون في البحث عن موردين بتكلفة طلب (S) أقل لزيادة الربحية.")

    if holding_cost > 20:
        st.write("3. **نصيحة تخزينية:** ابحث عن مستودع بتكلفة أقل أو حسن طرق التخزين لتقليل مصاريف الـ (H).")

# إضافة زر لتحميل التقرير (اختياري للإبهار)
st.sidebar.markdown("---")
if st.sidebar.button("📄 توليد تقرير القرار"):
    st.sidebar.write("تم تجهيز التقرير! (يمكنك ربطه هنا لطباعة النتائج)")
